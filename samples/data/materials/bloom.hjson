shader: shaders/post_process_bloom.hjson
blend_mode: {
  BlendMode: {
    colour_blend_op: add
    alpha_blend_op: add
    src_colour_factor: one
    dst_colour_factor: zero
    src_alpha_factor: one
    dst_alpha_factor: zero
  }
}
parameters: {
  sampler_bloom: {
    type: sampler2D
    value: "__default_texture_rgba_white"
  }
}
