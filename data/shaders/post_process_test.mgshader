// Syntax is GLSL-inspired, I recommend using GLSL-highlighting in your text editor.

// Tags tell the engine about how to interpret the shader.
TAGS {
}

// Options are #defines in shader code and may be used via '#if OPTION_NAME', etc
OPTIONS {
}

// Input parameters. All non-sampler parameters must specify default values. Valid types are float,
// vec2, vec3, and vec4.
// Samplers may be of type sampler2D or samplerCube.
PARAMETERS {
}

// Vertex code block. Consists of #include-directives and GLSL-code-literals.
// #include-directives must point to a GLSL source code file.
// GLSL-code-literals are delimited by BEGIN_GLSL and END_GLSL.
// Included GLSL-code and GLSL-code-literals are concatenated.
VERTEX_CODE {
}

// See comment for VERTEX_CODE above.
FRAGMENT_CODE {
#include "tone_map_post.glsl"
}
